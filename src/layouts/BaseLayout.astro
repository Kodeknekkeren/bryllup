---
const { title = "Bryllup" } = Astro.props;
---

<!doctype html>
<html lang="no">
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap"
      rel="stylesheet"
    />

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content="Praktisk info om bryllupet" />
    <meta name="robots" content="noindex,nofollow" />
    <style>
      :root {
        --max: 720px;
        --bg: #faf7f5;
        --text: #1a1a1a;
        --muted: #6b6b6b;
        --border: #e6e1dc;
        --accent: #b08968;
      }
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: "Playfair Display", "Inter", system-ui, sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.6;
      }

      body {
        overflow-x: hidden;
      }

      header {
        border-bottom: 1px solid var(--border);
        position: sticky;
        top: 0;
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(8px);
      }
      nav {
        max-width: var(--max);
        margin: 0 auto;
        padding: 12px 18px;
        display: flex;
        gap: 14px;
        flex-wrap: wrap;
      }
      nav a {
        color: var(--text);
        text-decoration: none;
        padding: 6px 10px;
        border-radius: 10px;
      }
      nav a:hover {
        background: #f6f6f6;
      }
      main {
        max-width: var(--max);
        margin: 0 auto;
        padding: 22px 18px 64px;
      }

      h3 {
        line-height: 1.2;
      }
      .muted {
        color: var(--muted);
      }
      footer {
        border-top: 1px solid var(--border);
        padding: 18px;
      }
      footer > div {
        max-width: var(--max);
        margin: 0 auto;
        color: var(--muted);
        font-size: 14px;
      }
      .card {
        border: 1px solid var(--border);
        border-radius: 20px;
        padding: 18px;
        background: white;
      }

      .grid {
        display: grid;
        gap: 14px;
      }

      /* Kart-container */
      .map {
        width: 100%;
        max-width: 100%;
        overflow-x: hidden;
      }

      .map iframe {
        display: block;
        width: 100%;
        max-width: 100vw; /* 🔑 DENNE */
        min-width: 0;
        border: 0;
      }

      .grid > * {
        min-width: 0;
      }

      /* 📱 MOBIL – TVING LAVERE KART */
      @media (max-width: 600px) {
        .map iframe {
          height: 220px !important;
        }
      }

      @media (min-width: 900px) {
        .map iframe {
          height: 420px;
        }
      }

      .mt {
        margin-top: 16px;
      }

      h1 {
        font-size: 2rem;
      }

      h2 {
        margin-top: 0;
      }
      h1,
      h2,
      h3 {
        font-family: "Playfair Display", serif;
      }

      @media (max-width: 375px) {
        nav {
          gap: 8px;
        }

        nav a {
          padding: 5px 9px;
          font-size: 13.5px;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <nav>
        <a href="/">Hjem</a>
        <a href="/program">Program</a>
        <a href="/praktisk">Praktisk</a>
        <a href="/onsker">Ønsker</a>
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer>
      <div>
        <span>❤️ Vi gleder oss til å feire med dere.</span>
      </div>
    </footer>
  </body>
</html>
